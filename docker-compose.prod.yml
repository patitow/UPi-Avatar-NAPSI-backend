# Producao: API so na rede Docker (Caddy faz o proxy publico).
services:
  api:
    ports: !reset []

  caddy:
    volumes:
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config:
